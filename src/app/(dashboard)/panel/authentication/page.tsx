import PanelAuthClient from '@/Components/dashboard/authentication';
import DashboardLayout from '@App/(dashboard)/layout';

const Page = () => {
  return <PanelAuthClient />;
};

export default Page;
