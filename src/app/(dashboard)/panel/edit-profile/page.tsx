import DashboardLayout from '@App/(dashboard)/layout';
import EditClientProfile from '@/Components/dashboard/edit-profile';

const Page = () => {
  // const handleSubmit = (values: {}) => {
  //   'use server';
  //   console.log(values);
  // };

  return <EditClientProfile />;
};

export default Page;
