import DashboardLayout from '@App/(dashboard)/layout';
import TicketPanelClient from '@/Components/dashboard/tickets';

const Tickets = () => {
  return <TicketPanelClient />;
};

export default Tickets;
